package dotProduct;

public class MatrixNode {
	public MatrixNode next;
	public MatrixNode previous;
	public int index;
	public int valueX;
	//public int valueY;
	
	/*
	 * public MatrixNode() { this.next = null; this.previous = null; this.index =
	 * -1; this.valueX = 0; this.valueY = 0; }
	 * 
	 * public MatrixNode(int index, int valueX) { this.next = null; this.previous =
	 * null; this.index = index; this.valueX = valueX; this.valueY = 0; }
	 * 
	 * public MatrixNode(int index, int valueX, MatrixNode previous) { this.next =
	 * next; this.previous = previous; this.index = index; this.valueX = valueX;
	 * this.valueY = 0; }
	 * 
	 * public MatrixNode(int index, int valueX, MatrixNode next, MatrixNode
	 * previous) { this.next = next; this.previous = previous; this.index = index;
	 * this.valueX = valueX; this.valueY = 0; }
	 */
}
